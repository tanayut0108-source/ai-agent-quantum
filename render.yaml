services:
  - type: web
    name: ai-agent-quantum
    runtime: python
    buildCommand: pip install .
    startCommand: uvicorn quantum_agent.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
    healthCheckPath: /health
    plan: free
